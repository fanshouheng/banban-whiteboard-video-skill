# Banban

First whiteboard-video visual prototype. Open `index.html` in a browser to preview the fixed 16:9 board and its annotation timeline.

The current sample is intentionally hand-authored. Batch JSON generation and audio synchronization are deferred until the visual format is approved.
